<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="85dp"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:padding="10dp">

    <TextView
        tools:text="Title"
        android:id="@+id/note_name"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/note_content"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/note_name"
        android:layout_below="@+id/note_name"
        android:paddingTop="5dp"
        android:textSize="14sp"
        tools:text="Content" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/note_content"
        android:layout_marginTop="10dp"
        android:background="#000000" />

    <ImageButton
        android:id="@+id/icon_delete_note"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_toRightOf="@id/note_content"
        android:scaleType="fitXY"
        android:background="#FFFFFF"
        android:src="@mipmap/baseline_delete_forever_black_48dp"/>
</RelativeLayout>